## API Report File for "@microsoft/agents-hosting-fastify"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ActivityHandler } from '@microsoft/agents-hosting';
import { AgentApplication } from '@microsoft/agents-hosting';
import { AuthConfiguration } from '@microsoft/agents-hosting';
import { CloudAdapter } from '@microsoft/agents-hosting';
import { CloudAdapterResult } from '@microsoft/agents-hosting';
import { ConversationState } from '@microsoft/agents-hosting';
import { createCloudAdapter } from '@microsoft/agents-hosting';
import { FastifyInstance } from 'fastify';
import type { FastifyPluginAsync } from 'fastify';
import type { FastifyReply } from 'fastify';
import type { FastifyRequest } from 'fastify';
import { FastifyServerOptions } from 'fastify';
import { RateLimitPluginOptions } from '@fastify/rate-limit';
import { TurnState } from '@microsoft/agents-hosting';
import type { WebResponse } from '@microsoft/agents-hosting';

// @public
export const adaptReply: (reply: FastifyReply) => WebResponse;

// @public
const agentsHostingFastifyPlugin: FastifyPluginAsync<AgentsHostingFastifyPluginOptions>;
export { agentsHostingFastifyPlugin }
export default agentsHostingFastifyPlugin;

// @public
export interface AgentsHostingFastifyPluginOptions {
    agent: AgentApplication<TurnState<any, any>> | ActivityHandler;
    authConfig?: AuthConfiguration;
    bodyLimit?: number;
    rateLimit?: RateLimitPluginOptions;
    routePath?: string;
}

export { CloudAdapterResult }

// @public
export const configureResponseController: (fastify: FastifyInstance, adapter: CloudAdapter, agent: ActivityHandler, conversationState: ConversationState) => void;

// @public
export const createAgentRequestHandler: (agent: AgentApplication<TurnState<any, any>> | ActivityHandler, authConfiguration?: AuthConfiguration) => FastifyAgentRequestHandler;

export { createCloudAdapter }

// @public
export type FastifyAgentRequestHandler = (request: FastifyRequest, reply: FastifyReply) => Promise<void>;

// @public
export class FastifyReplyAdapter implements WebResponse {
    constructor(reply: FastifyReply);
    // (undocumented)
    end(): this;
    // (undocumented)
    get headersSent(): boolean;
    // (undocumented)
    send(body?: unknown): this;
    // (undocumented)
    setHeader(name: string, value: string): this;
    // (undocumented)
    status(code: number): this;
    // (undocumented)
    get writableEnded(): boolean;
}

// @public
export function startServer(agent: AgentApplication<TurnState<any, any>> | ActivityHandler, options?: StartServerOptions): Promise<FastifyInstance>;

// @public (undocumented)
export function startServer(agent: AgentApplication<TurnState<any, any>> | ActivityHandler, authConfiguration?: AuthConfiguration): Promise<FastifyInstance>;

// @public
export interface StartServerOptions {
    authConfig?: AuthConfiguration;
    beforeListen?: (fastify: FastifyInstance) => void | Promise<void>;
    bodyLimit?: number;
    fastifyOptions?: FastifyServerOptions;
    port?: number | string;
    rateLimit?: RateLimitPluginOptions;
    routePath?: string;
}

// (No @packageDocumentation comment for this package)

```
